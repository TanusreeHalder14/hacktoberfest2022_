#include <bits/stdc++.h>
using namespace std;

int main()
{
    int tc;
    cin >> tc;
    while (tc--)
    {
        int x, y;
        cin >> x >> y;
        if (x == y)
        {
            cout << -1 << endl;
        }
        int a, b;
        a = x / 2;
        b = (x + 2) / 2;
        int c;
        if(y<x/2){
            c = y;
        }
        else{
            
        }
    }
    return 0;
}